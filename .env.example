# Copy to .env.local and fill in. .env.local is gitignored — never commit values.

# Supabase — project ref is ahumjhwqgmskjsitctcj
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Bypasses Row Level Security.
# Never expose this to the browser: no NEXT_PUBLIC_ prefix, never imported into
# a client component, never referenced outside src/lib/supabase/admin.ts.
SUPABASE_SERVICE_ROLE_KEY=

# --- Staging -----------------------------------------------------------------
#
# The second Supabase project — ref pblgpvcdappfpoxdascd — and the only database
# the QA harnesses are allowed to write to.
#
# They need one, and until it existed they had nowhere to go: every gate that
# builds fixtures signs up accounts, fills carts and places real orders, and the
# three variables above are the live shop. `scripts/audit/clients.ts` refuses to
# run them there, which is correct and which is also why the browser gates were
# blocked from Phase 7 onward.
#
# Set all three or none. Set all three and they take precedence over the three
# variables above for `scripts/audit/*` and for `npm run dev:stage`; set some of
# them and the harnesses stop with the name of the one that is missing, because
# a URL from one project and a key from another fails as a 401 a long way from
# where the mistake was made.
#
# In .env.local, NOT in a .env.staging file — nothing reads that, and Next
# ranks .env.local above every .env.<mode> anyway. The names are deliberately
# nothing like the three above: SUPABASE_STAGE_URL cannot be turned into
# NEXT_PUBLIC_SUPABASE_URL by a stray `cp`.
#
# The DB password is not read by the app. It is for `supabase db push --db-url`
# and psql — see docs/staging.md, which also lists the two extensions a fresh
# project needs enabled before the migrations will replay.
SUPABASE_STAGE_URL=
SUPABASE_STAGE_ANON_KEY=
SUPABASE_STAGE_SERVICE_ROLE_KEY=
SUPABASE_STAGE_DB_PASSWORD=

# Absolute origin, used for metadataBase, OG images and the sitemap.
# http://localhost:3000 in development.
#
# Not used for the OAuth redirect: that is built from the request's forwarded
# host instead, so every Vercel preview signs back in to itself rather than to
# production. This is the fallback for contexts with no request (build-time
# metadata, the sitemap).
NEXT_PUBLIC_SITE_URL=

# --- Google sign-in ----------------------------------------------------------
#
# Nothing goes here. The client ID and secret live in the Supabase dashboard
# (Authentication -> Providers -> Google), not in this app — the app never sees
# them, and never should. The redirect URLs that have to be allow-listed
# alongside them are listed in README.md under "What is blocked".

# --- Razorpay ----------------------------------------------------------------
#
# Names only. Values live in .env.local for development and in the Vercel
# project for deployments — set separately for Preview and Production, so a
# preview can never take a live payment.
#
# The key id is the publishable half of the pair. It reaches the browser, but
# only as part of a payment initiation the server returns — there is
# deliberately no NEXT_PUBLIC_ variable for it, which keeps it off every page
# that is not actually taking a payment.
#
# BOTH payment methods need these now. Pay on Delivery charges an advance
# online at checkout and the courier collects the balance, so it is a Razorpay
# payment too — src/lib/payments/cod.ts delegates to the Razorpay adapter and
# its isAvailable() is razorpayAdapter.isAvailable(). With these unset the
# checkout offers no method at all, which is the honest failure: a shop that
# cannot take money should say so before the address form, not after it.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# A DIFFERENT secret from RAZORPAY_KEY_SECRET, and the single most common thing
# to get wrong here. It signs the x-razorpay-signature header on incoming
# webhook events, and only that — verifying a webhook with the key secret fails
# every time and looks like a network problem.
#
# You invent this value; Razorpay does not generate it for you. Produce one with
# `openssl rand -hex 32`, then paste the same string into Razorpay Dashboard ->
# Account & Settings -> Website and app settings -> Webhooks -> Add New Webhook,
# and into the environment here. Use a different secret per environment.
#
# Leave it blank and the webhook route rejects everything with a 400, which is
# the correct direction to fail: an unverifiable endpoint that confirmed orders
# would confirm anybody's.
RAZORPAY_WEBHOOK_SECRET=

# --- Scheduled jobs -----------------------------------------------------------
#
# The bearer token on /api/cron/release-abandoned-orders, which is the half of
# the abandoned-order sweep that asks Razorpay whether a customer actually paid
# before anything gets cancelled. The route is public — anything on the internet
# can POST to it — so this token is the whole of its authentication, and an
# unset value makes the route refuse every request rather than accept them.
# Generate with `openssl rand -base64 32`.
#
# It has to be set in TWO places and they have to match:
#
#   1. here, and in the Vercel project (Production), which the route reads;
#   2. Supabase Vault, which pg_cron reads when it makes the call:
#        select vault.create_secret('<value>', 'cron_secret');
#        select vault.create_secret('https://www.footvault.in', 'cron_target_origin');
#
# pg_cron rather than Vercel Cron because the Vercel team is on the Hobby plan,
# where a sub-daily cron expression fails the deployment outright. See
# supabase/migrations/20260809030100_schedule_order_reconciler.sql.
#
# Rotating: change it in Vercel first, redeploy, then update the Vault secret.
# The other order leaves every tick 401ing until the deploy catches up, and this
# is the job that stops paid orders being cancelled.
CRON_SECRET=

# --- Email --------------------------------------------------------------------
#
# These are live now. Set BOTH or NEITHER: getEmailAdapter() in
# src/lib/email/index.ts returns the Resend adapter only when it has a key and a
# from-address, and falls back to the console adapter — logging loudly — if only
# one is present. Half a configuration would send from Resend's sandbox domain,
# which looks delivered and lands in spam.
#
# EMAIL_API_KEY  a Resend API key (re_...)
# EMAIL_FROM     the sender, e.g. "Foot Vault <orders@footvault.in>". The domain
#                must be the one verified in Resend, not a free mailbox.
#
# Verify the sending domain (SPF + DKIM) BEFORE setting either. Skipping that is
# what puts order confirmations in spam. Step-by-step: docs/admin-guide.md
# § Order emails.
#
# With neither set, every email is printed to the server log and no order is
# affected — that is the designed fallback, not a broken state.
EMAIL_API_KEY=
EMAIL_FROM=

# Where the shop owner's "a new order has arrived" copy goes. Unset means that
# email is skipped entirely — the customer's confirmation still sends, and
# checkout is unaffected either way. One address; use a forwarding alias if more
# than one person needs it.
OWNER_EMAIL=

# Signs the inbound webhook at /api/email/inbound. Resend shows it once, when
# the endpoint is created in the dashboard; it starts `whsec_`.
#
# Unset means the route answers 401 to everything — deliberately. The endpoint
# is public and it forwards mail *from our verified domain* into the owner's
# inbox, so an unauthenticated one is not a broken forwarder, it is an open
# relay carrying our DKIM signature. Refusing is the correct failure direction.
#
# Inbound MX is on the ROOT domain. Before any mailbox provider is added to
# footvault.in, that has to move to a subdomain — see docs/admin-guide.md.
RESEND_WEBHOOK_SECRET=

# --- Indexing ----------------------------------------------------------------
#
# Default, and the default for anything unset or misspelled, is noindex: the
# store is publicly reachable long before it is ready, and a page indexed with
# placeholder illustrations outlives the deploy that caused it.
#
# To go live: set this to exactly "true" in the Vercel project, Production only,
# so previews stay hidden.
#
# Then trigger a FRESH build -- push a commit, or redeploy with "Use existing
# Build Cache" UNCHECKED. A plain redeploy is not enough: next.config.ts's
# headers() is evaluated at build time and written into
# .next/routes-manifest.json, so an incremental build reuses the noindex header
# even though robots.txt correctly flips. Measured, not guessed.
#
# Then verify, because this fails silently:
#   curl -I https://<domain>/ | grep -i x-robots-tag   -> must print nothing
#   curl https://<domain>/robots.txt                   -> must show Allow: /
SITE_INDEXABLE=false

# --- Shiprocket ---------------------------------------------------------------
#
# NOT an API key. The value that sat in .env.local as SHIPROCKET_API_KEY through
# Phase 5 authenticates nothing -- it is 32 characters, not a JWT, and sending it
# as a bearer token to apiv2.shiprocket.in returns 401. That variable is gone;
# these two replace it.
#
# Shiprocket's external API authenticates with an API USER, not with the account
# you log into the panel with:
#
#   1. Shiprocket panel -> Settings -> API -> Configure -> Create an API user.
#   2. Its email MUST be different from the account's own login email. Using the
#      account email is the mistake everyone makes first, and it fails with a 403
#      that says nothing about why.
#   3. POST /v1/external/auth/login with these returns a JWT valid for 240 hours.
#      src/lib/shipping/token.ts is the only thing that calls it; the token is
#      cached in the integration_tokens table so a cold start does not log in.
#
# Leave both unset and the integration is off rather than broken: checkout still
# works, Pay on Delivery is still offered, and the delivery charge falls back to
# site_settings.shipping.fallback_fee_paise. No estimate is invented -- the page
# says the courier will quote at checkout rather than printing a number nobody
# gave us. A logistics outage must never block a sale, so an absent
# configuration must not either.
#
# The fallback is not a price list. Every real rate comes from the Shiprocket
# API and none is written down in this repository; the fallback exists only
# because refusing to sell during a courier outage is a worse outcome than
# mispricing a handful of orders, and it is a setting so the owner can correct
# it from /admin without a deploy.
SHIPROCKET_EMAIL=
SHIPROCKET_PASSWORD=

# The pickup location's nickname EXACTLY as it is spelled in the Shiprocket
# panel under Settings -> Company -> Pickup Addresses. The adhoc order payload
# matches it as a literal string, so a mismatch is rejected at order creation
# with a message about a pickup location rather than about a spelling.
#
# Blank here on purpose, and the code's own fallback is the reason to fill it in
# rather than a reason to skip it: shiprocketPickupLocation() returns "Primary"
# Phase 7: there is no default any more, and shiprocketPickupLocation() THROWS
# when this is unset. It used to fall back to "Primary", which is not what this
# account's location is called -- it is "warehouse", lowercase, confirmed
# against GET /v1/external/settings/company/pickup on 2026-08-08. Shiprocket
# matches the nickname as a literal string, so the fallback did not degrade
# gracefully: it deferred the failure to the moment a real parcel was being
# created for an order somebody had already paid for. Nothing on the customer
# path calls it -- quoting takes the pickup POSTCODE from site_settings -- so an
# unset variable stops the shop shipping rather than stopping it selling.
#
# Read yours from the panel, or with the token from /auth/login:
#   GET /v1/external/settings/company/pickup -> data.shipping_address[].pickup_location
#
# It must also agree with site_settings.shipping_defaults.pickup_postcode, which
# is the PIN serviceability is quoted FROM. Quoting from the wrong origin
# produces estimates that look completely reasonable and are for another city.
SHIPROCKET_PICKUP_LOCATION=

# ---------------------------------------------------------------------------
# The courier webhook's shared secret.
#
# Shiprocket's status-update webhook has no HMAC. Authentication is this token,
# in a header named by their portal's "Auth Token Type" dropdown -- leave that
# dropdown on its default, `Authorization`, and paste this value into the box
# beside it. The endpoint accepts it bare or as `Bearer <token>`.
#
# UNSET MEANS CLOSED. The route refuses every request when this is empty, which
# is the correct direction for an endpoint that can mark a parcel delivered and
# start a customer's 24-hour damage window. "Unset means open" is how a route
# quietly becomes public the first time a variable fails to copy across.
#
# Generate one that exists nowhere in this repository:
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
#
# It has to be identical in two places -- the Vercel project's environment and
# the Shiprocket portal -- and it is read at boot, so adding it needs a
# redeploy. See docs/shipping-webhook.md.
COURIER_WEBHOOK_TOKEN=
